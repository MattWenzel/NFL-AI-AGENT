# nflverse DB API + AI Chat Agent
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
anthropic>=0.39.0
sse-starlette>=1.8.0
tiktoken>=0.8.0

# Read-only stats database (single-file, columnar)
duckdb>=0.10.0

# Runtime persistence (ORM + async SQLite)
# Schema migrations are in-house via storage/schema_version.py (PRAGMA user_version).
sqlmodel>=0.0.16
aiosqlite>=0.19.0

# Auth + key encryption
cryptography>=42.0.0
bcrypt>=4.0.0

# Transactional email (verification + password reset)
resend>=0.10.0

# Error aggregation — no-op unless SENTRY_DSN is set
sentry-sdk[fastapi]>=2.0.0

# Google OAuth 2.0 + ID-token verification
authlib>=1.3.0

# Optional LLM providers (install for multi-provider support)
openai>=1.12.0
