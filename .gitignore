__pycache__/
*.pyc

# Database files
*.db
*.db-shm
*.db-wal
*.db.zip

# Backups
backups/

# API keys
api_keys.json
.env

# Runtime data (DB file + SQLite sidecars + pre-migration backups)
data/runtime.sqlite3*

# Local artifacts (not used by this branch, but may exist from other branches)
data/codex_auth.json
.codex

AGENT.md

# CSV exports (generated at runtime)
exports/

# NFLVERSE subproject (separate repo)
NFLVERSE/

# Tests (local-only; not shipped with the public repo)
tests/
.pytest_cache/

# Ad-hoc experiments (local research notebooks + raw SSE captures)
experiments/

# Claude Code local config
.claude/
